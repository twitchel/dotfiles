# dotfiles

## Requirements

### MacOS

A running system with the following installed
- homebrew package manager (brew)
- git

### Ubuntu
A running system with the following installed
- zsh (set as default shell)
- git

### Manjaro
A running system with the following installed
- zsh (set as default shell)
- git